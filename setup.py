from setuptools import setup

setup(name='ReconstrucTree',
      version='0.1',
      description='A tree skeleton reconstruction tool using Deep Learning',
      packages=['reconstructree'],
      install_requires=['numpy', 'matplotlib', 'keras'])
